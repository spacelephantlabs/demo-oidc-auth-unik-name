<%- include partials/dependencies.ejs %>
<%- include partials/navigation.ejs %>

<!-- Content -->
<div class="container content flex-fill d-flex flex-column">
  <div class="row">
      <div class="col-12 col-md-7 col-xl-6">
        <h1 class="mb-4">
          Welcome <%= user.username %>
        </h1>
        <h3 class="mb-2">
          Internal platform101 user's data
        </h3>
        <div class="mb-1">
          <span class="font-weight-bold">accountID: </span>
          <span class="font-weight-light"><%= user.id %></span>
        </div>
        <div class="mb-1">
          <span class="font-weight-bold">username: </span>
          <span class="font-weight-light"><%= user.username %></span>
        </div>
        <div class="mb-1">
          <span class="font-weight-bold">sign-up date: </span>
          <span class="font-weight-light"><%= user.signupDate %></span>
        </div>
        <div class="mb-1">
          <span class="font-weight-bold">last sign-in date: </span>
          <span class="font-weight-light"><%= user.lastSigninDate %></span>
        </div>
        <div>
          <span class="font-weight-bold">count of sign-in: </span>
          <span class="font-weight-light"><%= user.count %></span>
        </div>
      </div>
  </div>
</div>

<%- include partials/footer.ejs %>